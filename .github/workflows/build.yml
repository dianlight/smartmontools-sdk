name: Build and Release

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true
          fetch-depth: 0

      - name: Determine version from submodule
        id: version
        run: |
          cd smartmontools
          TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$TAG" ]; then
            TAG=$(git rev-parse --short HEAD)
          fi
          VERSION="${TAG#RELEASE_}"
          VERSION="${VERSION//_/.}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Detected version: ${VERSION}"

  build:
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux-amd64
            host: x86_64-linux-gnu
            cc: gcc
            cxx: g++
            cflags: ""
            cxxflags: ""
            deps: ""
            container: ""
            musl_toolchain: ""

          - os: ubuntu-latest
            target: linux-aarch64
            host: aarch64-linux-gnu
            cc: aarch64-linux-gnu-gcc
            cxx: aarch64-linux-gnu-g++
            cflags: ""
            cxxflags: ""
            deps: gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
            container: ""
            musl_toolchain: ""

          - os: ubuntu-latest
            target: linux-amd64-musl
            host: x86_64-linux-musl
            cc: x86_64-linux-musl-gcc
            cxx: x86_64-linux-musl-g++
            cflags: ""
            cxxflags: ""
            deps: ""
            container: ""
            musl_toolchain: x86_64-linux-musl-cross

          - os: ubuntu-latest
            target: linux-aarch64-musl
            host: aarch64-linux-musl
            cc: aarch64-linux-musl-gcc
            cxx: aarch64-linux-musl-g++
            cflags: ""
            cxxflags: ""
            deps: ""
            container: ""
            musl_toolchain: aarch64-linux-musl-cross

          - os: ubuntu-latest
            target: darwin-amd64
            host: x86_64-apple-darwin20.4
            cc: o64-clang
            cxx: o64-clang++
            cflags: "-arch x86_64"
            cxxflags: "-arch x86_64 -stdlib=libc++"
            deps: ""
            container: ghcr.io/smartmontools/docker-build:master
            musl_toolchain: ""

          - os: ubuntu-latest
            target: darwin-aarch64
            host: x86_64-apple-darwin20.4
            cc: o64-clang
            cxx: o64-clang++
            cflags: "-arch arm64"
            cxxflags: "-arch arm64 -stdlib=libc++"
            deps: ""
            container: ghcr.io/smartmontools/docker-build:master
            musl_toolchain: ""

          - os: ubuntu-latest
            target: windows-amd64
            host: x86_64-w64-mingw32
            cc: x86_64-w64-mingw32-gcc
            cxx: x86_64-w64-mingw32-g++
            cflags: ""
            cxxflags: ""
            deps: gcc-mingw-w64-x86-64 g++-mingw-w64-x86-64
            container: ""
            musl_toolchain: ""

    runs-on: ${{ matrix.os }}
    container: ${{ matrix.container || null }}
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
      SMARTMONTOOLS_TEST_BUILD: "1"

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: true
          fetch-depth: 0

      - name: Install dependencies
        if: matrix.deps != ''
        run: |
          sudo apt-get update
          sudo apt-get install -y ${{ matrix.deps }}

      - name: Cache musl toolchain
        if: matrix.musl_toolchain != ''
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: toolchain/${{ matrix.musl_toolchain }}
          key: musl-toolchain-${{ runner.os }}-${{ matrix.musl_toolchain }}-v1

      - name: Add musl toolchain to PATH
        if: matrix.musl_toolchain != ''
        run: echo "$GITHUB_WORKSPACE/toolchain/${{ matrix.musl_toolchain }}/bin" >> $GITHUB_PATH

      - name: Download musl toolchain if not cached
        if: steps.cache-toolchain.outputs.cache-hit != 'true' && matrix.musl_toolchain != ''
        run: |
          mkdir -p toolchain
          curl -sSL --retry 5 "https://musl.cc/${{ matrix.musl_toolchain }}.tgz" \
            | tar -xz -C toolchain

      - name: Setup Darwin cross-compiler PATH
        if: contains(matrix.target, 'darwin')
        run: |
          echo "/usr/osxcross/bin" >> $GITHUB_PATH
          echo "LD_LIBRARY_PATH=/usr/osxcross/lib" >> $GITHUB_ENV

      - name: Build library
        run: |
          ./autogen.sh --force
          mkdir -p build && cd build

          CONFIGURE_ARGS="--with-devel=yes --with-build-info='(GHA Build)'"

          if [ "${{ matrix.host }}" != "x86_64-linux-gnu" ]; then
            CONFIGURE_ARGS="${CONFIGURE_ARGS} --build=$(../config.guess) --host=${{ matrix.host }}"
          fi

          CC="${{ matrix.cc }}"
          CXX="${{ matrix.cxx }}"
          CFLAGS="${{ matrix.cflags }}"
          CXXFLAGS="${{ matrix.cxxflags }}"

          eval ../configure ${CONFIGURE_ARGS} \
            ${CC:+CC=\"$CC\"} \
            ${CXX:+CXX=\"$CXX\"} \
            ${CFLAGS:+CFLAGS=\"$CFLAGS\"} \
            ${CXXFLAGS:+CXXFLAGS=\"$CXXFLAGS\"}

          make -C include
          make -j$(nproc) -C lib libsmartmon.la

      - name: Package artifacts
        run: |
          mkdir -p artifacts/lib artifacts/include
          cp build/lib/.libs/libsmartmon.a artifacts/lib/
          cp -r include/smartmon artifacts/include/

      - name: Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: libsmartmon-${{ matrix.target }}
          path: artifacts/
          retention-days: 5

  release:
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    needs: [prepare, build]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.prepare.outputs.version }}
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Package release archives
        run: |
          mkdir -p release
          for dir in artifacts/libsmartmon-*; do
            target=$(basename "$dir" | sed 's/libsmartmon-//')
            tar -czf "release/libsmartmon-${VERSION}-${target}.tar.gz" -C "$dir" .
          done

      - name: Create or update release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete "v${VERSION}" --repo "${{ github.repository }}" --yes --cleanup-tag 2>/dev/null || true
          gh release create "v${VERSION}" \
            release/* \
            --repo "${{ github.repository }}" \
            --title "smartmontools-sdk v${VERSION}" \
            --notes "Library build from smartmontools ${VERSION}" \
            --latest
