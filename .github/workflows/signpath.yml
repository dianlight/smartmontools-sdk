# Disabled: This workflow was for signing Windows CLI installers (smartmontools).
# The smartmontools-sdk project builds a library (libsmartmon), not CLI tools.
# Re-enable and modify if library signing becomes necessary.

name: Sign windows binaries on signpath.io (DISABLED)

on: workflow_dispatch

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow is disabled - smartmontools-sdk builds a library, not CLI installers."
          exit 1
